{
  "name": "piratheon-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static blog for piratheon.github.io — Markdown/HTML in content/ compiled to static HTML with a client-side search index.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/build.mjs && npx --yes serve dist"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
